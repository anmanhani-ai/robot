import cv2

# ลองเปลี่ยนเลขตรงนี้ตามเลขที่เจอใน /dev/video*
camera_id = 0 
cap = cv2.VideoCapture(camera_id)

if not cap.isOpened():
    print(f"❌ เปิดกล้องเบอร์ {camera_id} ไม่ได้ ลองเปลี่ยนเลขดูครับ")
else:
    print(f"✅ เจอแล้ว! กล้องเบอร์ {camera_id} ใช้งานได้")
    
    # อ่านเฟรมมาโชว์ 1 รูปแล้วปิดเลย
    ret, frame = cap.read()
    if ret:
        cv2.imwrite("test_image.jpg", frame)
        print("📸 บันทึกภาพทดสอบชื่อ test_image.jpg เรียบร้อย")
    
    cap.release()